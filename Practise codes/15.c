#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char ch[100],ch[100];
    int i,j,count=0;
    gets(ch);
    int len=strlen(ch);
    for(i=len-1;i>=0;i--)
    {
        if()
    }

}